    {% if title %}
    <title>{{ title }}</title>
    {% else %}
    <title>Weather App</title>
    {% endif %}
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-KK94CHFLLe+nY2dmCWGMq91rCGa5gtU4mk92HdvYe+M/SXH301p5ILy+dN9+nJOZ" crossorigin="anonymous">

<body>
  <nav class="navbar navbar-expand-lg bg-body-tertiary mt-2">
  <div class="container-fluid">
    <a style="color:cornflowerblue"; class="navbar-brand" href="#">Weather App</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <a style="color:black; font"; class="nav-link active" aria-current="page" href="{{ url_for('main.weather') }}">Home</a>
        </li>
        {% if not current_user.is_authenticated %}
        <li class="nav-item">
          <a style="color: black"; class="nav-link" href="{{ url_for('auth.register') }}">Register</a>
        </li>
        <li class="nav-item">
          <a style="color: black"; class="nav-link" href="{{ url_for('auth.login') }}">Login</a>
        </li>
        {% else %}
        <li class="nav-item">
          <a style="color: black"; class="nav-link" href="{{ url_for('social.user_page', username=current_user.username) }}">My Logs</a>
        </li>
        <li class="nav-item">
          <a style="color: black;"; class="nav-link" href="{{ url_for('social.post') }}">Post</a>
        </li>
        <li class="nav-item">
          <a style="color: black;"; class="nav-link" href="{{ url_for('auth.logout') }}">Logout</a>
        </li>
        {% endif %}
      </ul>
    </div>
  </div>
</nav>

    </table>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ENjdO4Dr2bkBIFxQpeoTz1HIcje39Wm4jDKdf19U8gI4ddQ3GYNS7NTKfAdVQSZe" crossorigin="anonymous"></script>